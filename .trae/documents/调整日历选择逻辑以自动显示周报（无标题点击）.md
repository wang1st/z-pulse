## 目标
- 点击某天卡片：顶部晨报切换到该天；若该天有周报，则在日历选择器下方自动显示周报内容。
- 不在日历卡片内提供周报标题点击事件，周报标题仅用于提示（“+周报”+标题）。

## 修改点
### 1) 更改日历卡点击优先级
- 文件：frontend/app/components/ReportCalendar.tsx
- 位置：卡片整体点击处理函数 `handleClick`（约 209 行），当前签名：`const handleClick = (e: React.MouseEvent) => { ... }`
- 修改：
  - 将返回的报告优先级改为“周报优先”：`const report = weeklyReport || dailyReport`
  - 调用：`onDateClick(dateStr, report || null)` 保持不变。
- 目的：当某天存在周报时，卡片整体点击立即将下方显示切换为该周报；同时顶部晨报通过 `selectedDate` 切换到当天。

### 2) 去掉周报标题点击事件
- 文件：frontend/app/components/ReportCalendar.tsx
- 位置：周报标题渲染（约 241–257 行）
- 修改：
  - 保留“+ 周报”提示与周报标题的展示，但将标题从 `<button>` 改为纯文本（或 `<div>`），不绑定 `onClick`。
  - 删除 `e.stopPropagation()` 与 `onDateClick(dateStr, weeklyReport)` 的标题点击逻辑。
- 样式：
  - 标题可保持 `text-xs sm:text-sm font-medium text-purple-700 line-clamp-2`，但不再使用“hover:underline”等交互样式。

### 3) 顶部晨报联动保持不变
- 文件：frontend/app/page.tsx
- 位置：顶部晨报组件（约 47 行）
- 现状：`<DailyReportDisplay targetDate={selectedDate ?? undefined} />`
- 保持不变，确保顶部晨报跟随日历选中日期。

### 4) 下方内容渲染逻辑保持
- 文件：frontend/app/page.tsx
- 位置：日历下方渲染区域（约 59–144 行）
- 现状：当 `selectedReport` 为周报时渲染周报，否则渲染晨报或默认周报组件。
- 保持并验证在“周报优先”返回下能正确显示对应周报。

## 验证用例
- 仅晨报的日期：点击后顶部晨报切到该天；下方显示该晨报（无周报提示）。
- 晨报+周报的日期：点击后顶部晨报切到该天；下方直接显示该周报；卡片内仅提示“+周报”+标题但不可点击。
- 仅周报的日期：点击后顶部晨报若该天无晨报则不变；下方显示该周报。

## 备注
- 若需要“卡片整体点击仍优先晨报”，可保留现逻辑 `dailyReport || weeklyReport`，但你的最新要求明确希望自动显示周报，故选择“周报优先”。

请确认以上调整方案，我将据此更新 `ReportCalendar.tsx` 并进行本地验证。