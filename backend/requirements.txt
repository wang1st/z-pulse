# Web框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0  # Pydantic email验证支持

# 数据库
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Redis
redis==5.0.1

# HTTP客户端
httpx==0.26.0

# RSS解析
feedparser==6.0.11

# AI/ML - 阿里云Qwen + BERTopic
openai==1.10.0  # 用于阿里云Qwen API（兼容OpenAI接口）
bertopic==0.16.0  # 主题建模
sentence-transformers==2.3.1  # BERT embeddings
scikit-learn==1.4.0  # BERTopic依赖
jieba==0.42.1  # 中文分词 + 词性标注（用于日报混合NLP预处理）

# 邮件服务（支持多个提供商）
sendgrid==6.11.0
brevo-python==1.0.0  # Brevo (原Sendinblue) - 推荐，免费额度300封/天
jinja2==3.1.3
requests==2.31.0  # Mailgun需要

# 认证和安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# 定时任务
schedule==1.2.0

# 工具
python-dotenv==1.0.1
python-dateutil==2.8.2
pytz==2024.1
loguru==0.7.2

# 文件处理
pandas==2.2.0  # CSV/Excel处理
openpyxl==3.1.2  # Excel文件支持

# Markdown处理
markdown==3.5.2
beautifulsoup4==4.12.3
lxml==5.1.0

# PDF生成
weasyprint>=67.0

# 测试
pytest==7.4.4
pytest-asyncio==0.23.3
